<% if flash.any? %>
  <% flash.each do |key,msg| %>
    <div data-alert class="alert-box secondary">
      <%= content_tag(:span, msg, class: "label #{key}") %>
      <a href="#" class="close">&times;</a>
    </div>
  <% end %>
<% end %>
